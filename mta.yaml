_schema-version: "3.1"
ID: CAPM_incident_management
description: A simple CAP project.
version: 1.0.0
modules:
- name: CAPM_incident_management-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: CAPM_incident_management-db
  - name: CAPM_incident_management-auth
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    readiness-health-check-http-endpoint: /health
    readiness-health-check-type: http
  build-parameters:
    builder: npm
- name: CAPM_incident_management-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: CAPM_incident_management-db
  parameters:
    buildpack: nodejs_buildpack
- name: CAPM_incident_management-destination-content
  type: com.sap.application.content
  requires:
  - name: CAPM_incident_management-destination-service
    parameters:
      content-target: true
  - name: CAPM_incident_management_html_repo_host
    parameters:
      service-key:
        name: CAPM_incident_management_html_repo_host-key
  - name: CAPM_incident_management-auth
    parameters:
      service-key:
        name: CAPM_incident_management-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: incidents_CAPM_incident_management_html_repo_host
          ServiceInstanceName: CAPM_incident_management-html5-app-host-service
          ServiceKeyName: CAPM_incident_management_html_repo_host-key
          sap.cloud.service: incidents
        - Authentication: OAuth2UserTokenExchange
          Name: incidents_CAPM_incident_management_auth
          ServiceInstanceName: CAPM_incident_management-auth
          ServiceKeyName: CAPM_incident_management-auth-key
          sap.cloud.service: incidents
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: CAPM_incident_management-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: CAPM_incident_management_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nsincidents.zip
      name: nsincidents
      target-path: resources/
- name: nsincidents
  type: html5
  path: app/incidents
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: CAPM_incident_management-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: CAPM_incident_management-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: CAPM_incident_management-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: CAPM_incident_management-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: CAPM_incident_management-srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: CAPM_incident_management-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: CAPM_incident_management_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: CAPM_incident_management-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
